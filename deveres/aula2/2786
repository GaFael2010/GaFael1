#include <stdio.h>
 
int main() {
 
    int  c,l,j,o,n;
    double k,b;
    scanf("%d %d", &l, &c);
    
    o=(c*l)-1;
    n=c-1;
    j=l-1;

    
    k=o/1;
    

    b=(k-o)/0.5;

    printf("%d", n);


 
    return 0;
}
